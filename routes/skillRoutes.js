// const express = require('express');
// const router = express.Router();
// const { findUsersBySkill, addSkillToUser } = require('../controllers/skillController');
// const { authenticateToken } = require('../middlewares/authMiddleware');

// // Find users by skill (public route)
// router.get('/findBySkill', findUsersBySkill);

// // Add skill to authenticated user (protected route - requires authentication)
// router.post('/addSkill', authenticateToken, addSkillToUser);

// module.exports = router;
